# SEM EFEITO desde que o contexto de build do backend passou a ser a raiz do
# repositorio: o Docker le o `.dockerignore` da raiz do CONTEXTO, que agora e
# `/.dockerignore`. As regras abaixo ficam so como referencia — editar aqui nao
# muda o build.
node_modules
dist
*.log
.git
